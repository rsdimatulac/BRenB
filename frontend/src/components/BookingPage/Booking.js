// import { useParams } from "react-router-dom";

const Booking = () =>{
    // const { data } = useParams();
    // const newBooking = JSON.parse(data);
    // console.log("NEW BOOKINGGG", newBooking)


    // return dispatch(createBooking(newBooking));
    // usehistory to redirect

    return (
        <div>
            <h2>Booking here</h2>
        </div>
    )
}

export default Booking;
